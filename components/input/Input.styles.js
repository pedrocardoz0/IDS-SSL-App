import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputContainer: {
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 16,
    padding: 20,
    marginVertical: 10,
  },
});
