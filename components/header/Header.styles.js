import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
    padding: 24,
  },

  mainText: {
    fontWeight: 'bold',
    fontSize: 24,
  },

  subText: {
    fontSize: 16,
  },
});
